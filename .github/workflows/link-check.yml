name: Verificacion de enlaces

on:
  schedule:
    - cron: "0 9 * * 1"
  push:
    paths:
      - "**.md"
  workflow_dispatch:

jobs:
  link-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Revisar enlaces de los archivos markdown
        uses: lycheeverse/lychee-action@v2
        with:
          # 429 y 503 son rate limiting del runner, no enlaces rotos
          args: >-
            --no-progress
            --max-retries 3
            --retry-wait-time 5
            --accept 200,206,429,503
            "**/*.md"
          fail: true
